# Cyclone Air Classifier Simulation

GPU-accelerated simulation of cyclone air classifiers for particle separation using [NVIDIA Warp](https://github.com/NVIDIA/warp).

## Overview

This package simulates the operation of cyclone air classifiers, which separate particles by size using centrifugal forces generated by a swirling air flow. The simulation includes:

- **Geometry**: Parametric cyclone design with customizable dimensions
- **Fluid Dynamics**: Air flow field computation within the cyclone
- **Particle Tracking**: Lagrangian particle tracking with drag, gravity, and centrifugal forces
- **Separation Analysis**: Grade efficiency curves and cut size calculations

## Project Structure

```text
airclassifier/
├── src/airclassifier/
│   ├── geometry/       # Cyclone geometry and mesh generation
│   ├── fluid/          # Flow field solvers and turbulence models
│   ├── particles/      # Material properties and particle systems
│   ├── kinetics/       # Force calculations and trajectory integration
│   ├── simulation/     # Main simulation orchestration
│   ├── io/             # Configuration and data I/O
│   ├── visualization/  # Rendering and plotting
│   └── utils/          # Constants, units, validation
├── config/             # Configuration files
├── examples/           # Example simulation scripts
├── notebooks/          # Jupyter notebooks for analysis
└── tests/              # Test suite
```

## Installation

```bash
# Clone the repository
cd airclassifier

# Create virtual environment
python -m venv venv
source venv/bin/activate  # Linux/Mac
# or: venv\Scripts\activate  # Windows

# Install dependencies
pip install -r requirements.txt

# Install package in development mode
pip install -e .
```

## Quick Start

Remove-Item -Path "C:\Users\Windows\AppData\Local\NVIDIA\warp\Cache\1.11.0" -Recurse -Force -ErrorAction SilentlyContinue

```bash
# Full-system classification (air -> feed -> venturi -> classification)
# Bench-scale geometry (40 mm venturi, 200 mm wheel): use blower 400–600 RPM and wheel 2000–4000 RPM for best protein/starch separation without venturi choke.
python examples/run_classification_flow.py --blower-rpm 500 --wheel-rpm 3000 --full-system --material yellow_pea
```

```python
import warp as wp
from airclassifier.geometry import CycloneAssembly
from airclassifier.simulation import Simulator

# Initialize Warp
wp.init()

# Load configuration
config = "config/default_config.yaml"

# Create and run simulation
sim = Simulator.from_config(config)
sim.run()

# Analyze results
sim.plot_separation_efficiency()
```

## Requirements

- Python >= 3.10
- NVIDIA GPU with CUDA support (recommended)
- NVIDIA Warp >= 1.11.0

## License

MIT License
